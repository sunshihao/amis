import { CT_AutoFilter } from '../../../openxml/ExcelTypes';
import { Sheet } from '../../sheet/Sheet';
/**
 * 渲染 AutoFilter 图标
 */
export declare function renderAutoFilterIcon(sheet: Sheet, autoFilter: CT_AutoFilter, id: string, dataContainer: HTMLElement, headerRowCount?: number): void;
