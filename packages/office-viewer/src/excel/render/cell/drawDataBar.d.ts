import { DataBarDisplay } from '../../types/DataBarDisplay';
import { Canvas } from '../Canvas';
export declare function drawCellDataBar(canvas: Canvas, dataBarDisplay: DataBarDisplay, x: number, y: number, width: number, height: number): void;
