import { IconNames } from '../../io/excel/preset/presetIcons';
import { Canvas } from '../Canvas';
export declare function drawIconSet(canvas: Canvas, icon: IconNames, x: number, y: number, width: number, height: number): void;
