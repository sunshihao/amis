import { Size } from '../../sheet/ViewRange';
/**
 * 二分查找到到 hitTest 的位置
 * @param sizes 位置信息
 * @param position 要查找的位置
 * @returns
 */
export declare function binarySearchSize(sizes: Size[], position: number): number;
