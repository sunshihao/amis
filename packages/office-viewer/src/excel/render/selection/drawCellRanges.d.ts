import { Workbook } from '../../Workbook';
import { Canvas } from '../Canvas';
import { SheetSelection } from './SheetSelection';
/**
 * 绘制选择区域
 * @param workbook
 * @param canvas
 * @param selection
 */
export declare function drawCellRanges(workbook: Workbook, canvas: Canvas, selection: SheetSelection): void;
