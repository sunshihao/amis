import { Workbook } from '../../Workbook';
import { Canvas } from '../Canvas';
/**
 * 绘制选中区表头的高亮
 */
export declare function drawSelectionHeaderHighlight(workbook: Workbook, canvas: Canvas, x: number, y: number, width: number, height: number): void;
